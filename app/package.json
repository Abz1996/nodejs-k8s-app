{
  "name": "nodejs-k8s-app",
  "version": "1.0.0",
  "description": "Node.js app for K8s deployment via Jenkins",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Test passed\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}